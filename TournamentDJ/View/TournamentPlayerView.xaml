<UserControl x:Class="TournamentDJ.View.TournamentPlayerView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:local="clr-namespace:TournamentDJ.View"
      xmlns:ess="clr-namespace:TournamentDJ.Essentials"
      xmlns:vm="clr-namespace:TournamentDJ.ViewModel"
      xmlns:conv="clr-namespace:TournamentDJ.Converter"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=vm:TournamentPlayerViewModel, IsDesignTimeCreatable=False}"
      d:DesignHeight="300" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/TournamentDJ;component/Styles/SliderStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <conv:UriToStringConverter x:Key="UriToStringConverter" />
            <ess:BindingProxy x:Key="BindingProxy" Data="{Binding}"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="Bisque">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*" MinWidth="300"/>
            <ColumnDefinition Width="1*" MinWidth="90"/>
            <ColumnDefinition Width="1*" MinWidth="90"/>
        </Grid.ColumnDefinitions>
        <StackPanel MinHeight="200" MinWidth="300" MaxHeight="300">
            <DockPanel MaxHeight="70" Background="NavajoWhite">
                <TextBlock HorizontalAlignment="Left" Width="100" Height="50" Margin ="5" Background="Azure" Text="{Binding Player.TrackPlaying.Uri, Converter={StaticResource UriToStringConverter}}" TextWrapping="Wrap" />
                <TextBlock HorizontalAlignment="Left" Width="100" Height="50" Margin ="5" Background="Azure" Text="{Binding Player.timeData}" />
                <TextBlock HorizontalAlignment="Left" Width="100" Height="50" Margin ="5" Background="Azure" Text="{Binding Player.TrackPlaying.Title}" TextWrapping="Wrap" />
                <TextBlock HorizontalAlignment="Left" Width="100" Height="50" Margin ="5" Background="Azure" Text="{Binding Player.SelectedAudioDevice.Name}" TextWrapping="Wrap" />
                <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                    <TextBlock Text="Laufzeit"/>
                    <ComboBox Name="comboBox1"
                        ItemsSource="{Binding Runtimes}"
                        SelectedItem="{Binding SelectedTimeSpan}"
                        Text="auswählen..."
                        IsEditable="true"
                        IsReadOnly="true"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                    <TextBlock Text="Tanzrunde"/>
                    <ComboBox Name="comboBox2"
                        ItemsSource="{Binding DanceRounds}"
                        SelectedItem="{Binding SelectedDanceRound}"
                        DisplayMemberPath="Name"
                        Text="auswählen..."
                        IsEditable="true"
                        IsReadOnly="true"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                    <TextBlock Text="Anzahl Heats"/>
                    <ComboBox Name="comboBox3"
                        ItemsSource="{Binding NumberOfHeats}"
                        SelectedItem="{Binding SelectedNumberOfHeats}"
                        Text="auswählen..."
                        IsEditable="true"
                        IsReadOnly="true"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                    <TextBlock Text="Difficutly"/>
                    <ComboBox
                        ItemsSource="{Binding Difficulties}"
                        DisplayMemberPath="Value"
                        SelectedValuePath="Key"
                        SelectedValue="{Binding Player.TrackPlaying.Difficulty}"
                        IsEditable="true"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                    <TextBlock Text="Characteristic"/>
                    <ComboBox
                        ItemsSource="{Binding Characteristics}"
                        DisplayMemberPath="Value"
                        SelectedValuePath="Key"
                        SelectedValue="{Binding Player.TrackPlaying.Characteristic, Mode=TwoWay}"
                        IsEditable="true"
                         />
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                    <TextBlock Text="Single"/>
                    <CheckBox IsChecked="{Binding IsSingleSelected}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                    <TextBlock Text="Mark as Favorite"/>
                    <CheckBox IsChecked="{Binding Player.TrackPlaying.FlaggedAsFavourite}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                    <TextBlock Text="Mark for Review"/>
                    <CheckBox IsChecked="{Binding Player.TrackPlaying.FlaggedForReview}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Left" MinWidth="300" MinHeight="100">
                    <TextBlock Text="Comment"/>
                    <TextBox Text="{Binding Player.TrackPlaying.Comment, Mode=TwoWay}"/>
                </StackPanel>
            </DockPanel>
            <DockPanel Grid.Column="1" HorizontalAlignment="Left" MaxHeight="70">
                <Button Margin="5" x:Name="Play" Content="Play" Width="50" Height="50" HorizontalAlignment="Left" Command="{Binding PlayClickCommand}"/>
                <Button Margin="5" x:Name="Stop" Content="Stop" Width="50" Height="50" HorizontalAlignment="Left" Command="{Binding StopClickCommand}"/>
                <Button Margin="5" x:Name="Fadeout" Content="Fadeout" Width="50" Height="50" HorizontalAlignment="Left" Command="{Binding FadeoutClickCommand}"/>
                <Button Margin="5" x:Name="Reselect" Content="Reselect" Width="50" Height="50" HorizontalAlignment="Left" Command="{Binding ReselectClickCommand}" CommandParameter="{Binding Player.TrackPlaying}"/>
                <Button Margin="5" x:Name="OpenFile" Content="OpenFile" Width="50" Height="50" HorizontalAlignment="Left" Command="{Binding OpenFileCommand}"/>
                <Button Margin="5" x:Name="CreateDanceRound" Content="Create Dance Round" Width="50" Height="50" HorizontalAlignment="Left" Command="{Binding CreateDanceRoundClickCommand}"/>
            </DockPanel>
            <ScrollViewer VerticalScrollBarVisibility="Visible" MinHeight="100" MaxHeight="140" Grid.Column="0" MinWidth="300" Background="Green">
                <ItemsControl ItemsSource="{Binding TracksToPlay.Tracks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <DockPanel>
                                <TextBlock Width="100" Height="50" Margin ="5" Background="Azure" Text="{Binding Title}" TextWrapping="Wrap" />
                                <TextBlock Width="200" Height="50" Margin ="5" Background="Azure" Text="{Binding Uri, Converter={StaticResource UriToStringConverter}}" TextWrapping="Wrap" />
                                <TextBlock Width="100" Height="50" Margin ="5" Background="Azure" Text="{Binding Dance.Name}" TextWrapping="Wrap" />
                                <Button Content="Reselect" Width="100" Height="50" HorizontalAlignment="Right" Command="{Binding Data.ReselectClickCommand, Source={StaticResource BindingProxy}}" CommandParameter="{Binding}"/>
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
        <DockPanel Width="200" HorizontalAlignment="Right" Grid.Column="1">
            <Slider Ticks="20" Width="200" Height="30" HorizontalAlignment="Right" Value="{Binding Player.MedPlayer.Volume}" Maximum="1.0" LargeChange="0.1">
                <Slider.LayoutTransform>
                    <RotateTransform CenterX="0" CenterY="0" Angle="-90"/>
                </Slider.LayoutTransform>
                <i:Interaction.Behaviors>
                    <ess:MouseWheelSliderBehavior Amount="0.03" />
                </i:Interaction.Behaviors>
            </Slider>
        </DockPanel>
    </Grid>
</UserControl>
